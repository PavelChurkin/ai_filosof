# AI Filosof - Telegram Bot

Телеграм-бот философ, который генерирует философские мысли по запросу.

## Основные возможности

- 🧠 Генерация философских мыслей по запросу пользователя
- 💭 Трёхэтапный процесс генерации мыслей:
  1. Случайные слова → образ и роль
  2. Образ → вопрос
  3. Вопрос → ответ
- 💰 Система лимитов и пожертвований:
  - Минимум 3 запроса в день (восстанавливаются ежедневно)
  - Пожертвования увеличивают баланс запросов (50₽ = +3 запроса)
- 🗄️ База данных для хранения истории мыслей и состояния чатов
- 👥 Поддержка приватных чатов, групп и каналов

## Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/PavelChurkin/ai_filosof.git
cd ai_filosof
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

4. Заполните `.env` файл своими ключами:
- `OPENAI_API_KEY` - ключ OpenAI API
- `TG_TOKEN` - токен Telegram бота (получить у @BotFather)
- `YOOKASSA_SHOP_ID` - ID магазина YooKassa
- `YOOKASSA_SECRET_KEY` - секретный ключ YooKassa

## Запуск

```bash
python filosof_bot.py
```

## Использование

### Для пользователей

1. Добавьте бота в чат или начните приватную переписку
2. Отправьте команду `/start`
3. Используйте inline-кнопки для генерации мыслей:
   - ⚡ **Срочная мысль** - генерация мысли по стандартному алгоритму
   - 🎲 **Свои случайные слова** - генерация мысли на основе ваших слов
   - ❓ **Ваш вопрос** - прямой ответ на ваш философский вопрос
   - 💝 **Пожертвование** - поддержка проекта и увеличение баланса запросов

### Особенности работы

- Все чаты (приватные, группы, каналы) имеют одинаковый функционал
- Минимум 3 запроса в день доступно каждому чату
- Пожертвования увеличивают баланс запросов (50₽ = +3 запроса)
- Баланс восстанавливается ежедневно до минимума 3 запросов

### Структура проекта

```
ai_filosof/
├── filosof_bot.py       # Основной файл бота
├── database.py          # Модели базы данных и утилиты
├── payments.py          # Интеграция с YooKassa
├── filosof.py          # Старая версия (legacy)
├── requirements.txt     # Зависимости
├── .env.example        # Пример конфигурации
├── nouns.json          # Словарь существительных
├── verbs.json          # Словарь глаголов
├── adject.json         # Словарь прилагательных
└── README_NEW.md       # Документация
```

## База данных

Бот использует SQLite (через SQLAlchemy) для хранения:

- **chat_states** - состояние каждого чата (тип чата, баланс запросов)
- **thoughts** - история всех сгенерированных мыслей с 3 этапами
- **payments** - история платежей через Robokassa

## Архитектура генерации мыслей

Бот использует трёхэтапный процесс генерации:

1. **Этап 1**: Выбор случайных слов из словарей → генерация образа и определение роли (до 100 слов)
2. **Этап 2**: На основе образа → формулирование вопроса
3. **Этап 3**: На основе вопроса → генерация ответа (до 100 слов) - это и есть публикуемая мысль

Все этапы сохраняются в базе данных и могут быть раскрыты за отдельную плату.

## Система лимитов

- Каждый чат (приватный, группа, канал) имеет свой баланс запросов
- Минимум 3 запроса в день доступно автоматически
- Каждый запрос (срочная мысль, свои слова, вопрос) расходует 1 запрос
- Ежедневно баланс восстанавливается до минимума 3 запросов
- Пожертвования увеличивают баланс: 50₽ = +3 запроса

## Платежи

Интеграция с Robokassa для приёма пожертвований:

- Минимальная сумма пожертвования: 50₽
- За каждые 50₽ пользователь получает +3 запроса к балансу
- Доступны предустановленные суммы: 50₽, 100₽, 200₽, 500₽, 1000₽
- Возможность ввести произвольную сумму

Все платежи сохраняются в базе данных с метаданными.

## Разработка

### Требования

- Python 3.8+
- SQLite 3
- Telegram Bot API
- OpenAI API
- Robokassa API

### Тестирование

```bash
# TODO: Добавить тесты
python -m pytest tests/
```

## Лицензия

См. файл LICENSE

## Авторы

- Оригинальный код: PavelChurkin
- Рефакторинг в Telegram бота: AI Issue Solver
